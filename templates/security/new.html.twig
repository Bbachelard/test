{% extends 'base.html.twig' %}

{% block title %}Hello SecurityController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Creer un compte | <a href="{{path('login')}}" class="btn btn-sn btn-primary">Se connecter</a></h1>

    <div class="row"> 
      <div class="row">
            <div class="col-6 col-md-8">
               
               {% include "includes/_alert.html.twig" %}
            

                {{ form_start(form)}}
                    <div class="form-group mb-3">
                        <label for="inputEmail">Adresse Email</Label><br>
                        {{ form_widget(form.email) }}
                        <small id="emailHelp" class="form-text text-muted">Veuillez conserver votre adresse uniquement vous et pas le partager</small>
                    </div>
                    <div class="form-group mb-3">
                        <label for="inputEmail">Numéro de tel</Label>
                        {{ form_widget(form.tel) }}
                    </div>
                    <div class="form-group mb-3">
                        <label for="inputEmail">Username</Label>
                        {{ form_widget(form.username) }}
                    </div>
                    <div class="form-qroup mb-3">
                        <Label for="1nputPassword">mot de passe</Label>
                        {{ form_widget(form.password) }}
                    </div>
                    <div class="form-qroup mb-3">
                        {{ form_widget(form.passwordConfirm) }}
                    </div>
                    <button type="submit" class="btn btn-primary">Se Connecter</button>
                {{ form_end(form)}}
                
            </div>
        </div>
    </div>
      
</div>
{% endblock %}