{% extends 'base.html.twig' %}

{% block title %}Hello SecurityController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Se connecter | <a href="{{path('app_login_new')}}" class="btn btn-sn btn-primary">Creer</a></h1>

    {% include "includes/_alert.html.twig" %}
    <div class="row">
            <div class="col-6 col-md-8">
                <form action="{{ path('login')}}" method="post">
                    <div class="form-group mb-2">
                        <label for="inputEmail">Adresse Email</Label>
                        <input type="email" name="_username" value="{{ lastUsername }}" class="form-control" id="inputEmail" placeholder="Indiquer votre email">
                        <small id="emailHelp" class="form-text text-muted">Veuillez conserver votre adresse uniquement vous et pas le partager</small>
                    </div>
                    <div class="form-qroup mb-2">
                        <Label for="1nputPassword">mot de passe</Label>
                        <input type="password" class="form-control" name="_password"id="inputPassword" placeholder="Indiquez votre mot de passe">
                    </div>
                    <button type="submit" class="btn btn-primary">Se Connecter</button>
                </form>
            </div>
        </div>
    
</div>
{% endblock %}
